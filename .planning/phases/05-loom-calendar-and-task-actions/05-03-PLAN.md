---
phase: 05-loom-calendar-and-task-actions
plan: 03
type: execute
wave: 3
depends_on: ["05-02"]
files_modified:
  - LoomCal/Views/Chat/ActionConfirmationCard.swift
  - LoomCal/Views/Chat/UndoBanner.swift
  - LoomCal/Views/Chat/ChatView.swift
  - LoomCal/Views/Calendar/TimelineEventCard.swift
  - LoomCal/Views/Tasks/TaskRowView.swift
  - LoomCal/Views/ContentView.swift
autonomous: false
requirements: [LOOM-05, LOOM-06, LOOM-07]

must_haves:
  truths:
    - "Pending action messages render as confirmation cards in the chat stream, not as regular bubbles"
    - "User can tap Confirm on a card to execute the mutation and see the result in the calendar or task list"
    - "User can tap Cancel on a card to dismiss it without executing the mutation"
    - "After confirming, an undo banner appears at the bottom of chat for ~5 seconds"
    - "After a mutation completes, the affected event or task pulses with a highlight animation"
    - "Confirmed and cancelled cards show their final status instead of action buttons"
    - "Edit actions show a before/after diff in the confirmation card"
  artifacts:
    - path: "LoomCal/Views/Chat/ActionConfirmationCard.swift"
      provides: "Confirmation card view with action preview, Confirm/Cancel buttons, before/after diff, status states"
      contains: "ActionConfirmationCard"
    - path: "LoomCal/Views/Chat/UndoBanner.swift"
      provides: "Undo banner with countdown timer and undo button"
      contains: "UndoBanner"
    - path: "LoomCal/Views/Chat/ChatView.swift"
      provides: "Integration of ActionConfirmationCard for pending_action messages and UndoBanner overlay"
      contains: "ActionConfirmationCard"
    - path: "LoomCal/Views/Calendar/TimelineEventCard.swift"
      provides: "Highlight pulse animation when event matches highlightedEventId"
      contains: "highlightPulse"
    - path: "LoomCal/Views/Tasks/TaskRowView.swift"
      provides: "Highlight pulse animation when task matches highlightedTaskId"
      contains: "highlightPulse"
  key_links:
    - from: "LoomCal/Views/Chat/ChatView.swift"
      to: "ActionConfirmationCard"
      via: "conditional rendering based on message.role == pending_action"
      pattern: "pending_action"
    - from: "ActionConfirmationCard Confirm button"
      to: "ChatViewModel.confirmAction"
      via: "onConfirm closure calling chatViewModel.confirmAction(message)"
      pattern: "confirmAction"
    - from: "ChatView UndoBanner"
      to: "ChatViewModel.undoAction"
      via: "onUndo closure calling chatViewModel.undoAction()"
      pattern: "undoAction"
    - from: "TimelineEventCard highlightPulse"
      to: "CalendarViewModel.highlightedEventId"
      via: "@ObservedObject calendarViewModel binding"
      pattern: "highlightedEventId"
---

<objective>
Build the SwiftUI confirmation card, undo banner, and highlight animation views, and integrate them into the existing chat and calendar/task views.

Purpose: Complete the user-facing Phase 5 experience: pending action messages render as tappable confirmation cards in chat, confirmed actions show an undo banner, and mutations trigger a highlight pulse on the affected calendar event or task row.

Output: ActionConfirmationCard view, UndoBanner view, updated ChatView with action card routing and undo overlay, updated TimelineEventCard and TaskRowView with highlight pulse modifier.
</objective>

<execution_context>
@/Users/criswo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/criswo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-loom-calendar-and-task-actions/05-RESEARCH.md
@.planning/phases/05-loom-calendar-and-task-actions/05-01-SUMMARY.md
@.planning/phases/05-loom-calendar-and-task-actions/05-02-SUMMARY.md
@LoomCal/Views/Chat/ChatView.swift
@LoomCal/Views/Chat/ChatBubbleView.swift
@LoomCal/Views/Calendar/TimelineEventCard.swift
@LoomCal/Views/Tasks/TaskRowView.swift
@LoomCal/Views/ContentView.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ActionConfirmationCard and UndoBanner views</name>
  <files>LoomCal/Views/Chat/ActionConfirmationCard.swift, LoomCal/Views/Chat/UndoBanner.swift</files>
  <action>
**LoomCal/Views/Chat/ActionConfirmationCard.swift** (new file) — A card view rendered inline in the chat stream for pending_action messages. Design per user's locked decisions: stays in chat bubble stream (not a modal/sheet).

Layout:
- Left-aligned (like Loom bubbles), with slight indentation
- Rounded rectangle card with subtle border and background (Color.gray.opacity(0.08) fill, Color.gray.opacity(0.2) stroke)
- Content sections:
  1. **Header**: Icon + action type label (e.g., calendar.badge.plus for create_event, pencil for update, trash for delete, checkmark.circle for task) — use SF Symbols
  2. **Summary**: `action.displaySummary` text in `.body` font
  3. **Details section** (varies by action type):
     - For create_event: Show title, date/time, duration, location if present
     - For create_task: Show title, priority, due date if present
     - For update_event/update_task: Show before/after diff — old value with `.strikethrough()` in `.secondary` color, new value in primary color. Use `action.previousValues` dict keys to determine which fields changed.
     - For delete_event/delete_task: Show the item being deleted (title)
  4. **Action buttons** (only when `actionStatus == "pending"`):
     - "Confirm" button: `.borderedProminent` style, accent color
     - "Cancel" button: `.bordered` style, secondary color
     - Buttons in an HStack with Spacer

- When `actionStatus == "confirmed"`: Show green checkmark + "Confirmed" text, no buttons
- When `actionStatus == "cancelled"`: Show gray X + "Cancelled" text, no buttons
- When `actionStatus == "undone"`: Show orange arrow.uturn.backward + "Undone" text, no buttons

Props: `message: ChatMessage`, `onConfirm: () -> Void`, `onCancel: () -> Void`

Format dates from payload for display: convert millisecond timestamp strings to readable dates using DateFormatter. Helper function `formatActionDate(_ msString: String?) -> String?` that converts string→Int→Date→formatted string.

**LoomCal/Views/Chat/UndoBanner.swift** (new file) — A persistent strip shown at the bottom of chat above the input bar during the undo window.

Layout:
- HStack: action summary text (`.lineLimit(1)`) + Spacer + "Undo (Ns)" button
- Background: `Color.gray.opacity(0.15)` (cross-platform) with rounded corners
- Horizontal padding, vertical padding 10
- Transition: `.move(edge: .bottom).combined(with: .opacity)` for smooth appearance/disappearance

Props: `displaySummary: String`, `secondsRemaining: Int`, `onUndo: () -> Void`

Register both files in the Xcode project file (pbxproj) in the Views/Chat group.
  </action>
  <verify>
Build: `xcodebuild build -project "/Volumes/Blume/Wo Code/Loom Cal/LoomCal.xcodeproj" -scheme LoomCal -destination "platform=iOS Simulator,name=iPhone 16 Pro" -quiet 2>&1 | tail -5`. Verify BUILD SUCCEEDED. Verify both files exist and are registered in pbxproj.
  </verify>
  <done>
ActionConfirmationCard renders action previews with type-specific icons, detail fields, before/after diffs for edits, and Confirm/Cancel buttons that transition to status labels on state change. UndoBanner shows a countdown strip with undo button. Both files registered in pbxproj. Swift builds clean.
  </done>
</task>

<task type="auto">
  <name>Task 2: Integrate action cards and undo banner into ChatView, add highlight pulse to event and task views</name>
  <files>LoomCal/Views/Chat/ChatView.swift, LoomCal/Views/Calendar/TimelineEventCard.swift, LoomCal/Views/Tasks/TaskRowView.swift, LoomCal/Views/ContentView.swift</files>
  <action>
**LoomCal/Views/Chat/ChatView.swift** — Modify the message rendering loop and add undo banner:

1. In the `ForEach(group.messages)` loop, replace the unconditional `ChatBubbleView(message: message)` with a conditional:
```swift
if message.role == "pending_action" {
    ActionConfirmationCard(
        message: message,
        onConfirm: { chatViewModel.confirmAction(message) },
        onCancel: { chatViewModel.cancelAction(message) }
    )
    .id(message.id)
} else {
    ChatBubbleView(message: message)
        .id(message.id)
}
```

2. Add the UndoBanner between the message list and the input bar. Wrap the existing structure so the undo banner appears above the Divider + ChatInputBar:
```swift
// After ScrollView, before Divider
if let undo = chatViewModel.activeUndoAction {
    UndoBanner(
        displaySummary: undo.action.displaySummary,
        secondsRemaining: chatViewModel.undoSecondsRemaining,
        onUndo: { chatViewModel.undoAction() }
    )
    .animation(.easeInOut(duration: 0.2), value: chatViewModel.activeUndoAction != nil)
}
```

Note: `activeUndoAction` is a tuple `(action: LoomAction, messageId: String, createdId: String?)`. Access `.action.displaySummary` for the summary text.

3. ChatView needs access to CalendarViewModel and TaskViewModel to pass through for the card (or just let ChatViewModel handle everything via its stored references). ChatView should accept `@ObservedObject var chatViewModel: ChatViewModel` (already does). No additional ViewModel references needed in ChatView itself — the ChatViewModel methods handle the mutations internally.

**LoomCal/Views/Calendar/TimelineEventCard.swift** — Add highlight pulse:

1. Add a `HighlightPulseModifier` ViewModifier (or use inline overlay):
```swift
struct HighlightPulseModifier: ViewModifier {
    let isHighlighted: Bool
    @State private var pulseOpacity: Double = 0

    func body(content: Content) -> some View {
        content
            .overlay {
                if isHighlighted {
                    RoundedRectangle(cornerRadius: 6)
                        .stroke(Color.accentColor, lineWidth: 2)
                        .opacity(pulseOpacity)
                        .onAppear {
                            withAnimation(.easeInOut(duration: 0.5).repeatCount(3, autoreverses: true)) {
                                pulseOpacity = 1.0
                            }
                        }
                }
            }
    }
}
```

2. TimelineEventCard needs to know if it's highlighted. Add a parameter `isHighlighted: Bool = false` and apply the modifier at the end of the card body. The caller (DayTimelineView, WeekTimelineView) passes `isHighlighted: calendarViewModel.highlightedEventId == event._id`.

3. Update all call sites of TimelineEventCard in DayTimelineView and WeekTimelineView (and TodayView if it uses TimelineEventCard) to pass the `isHighlighted` parameter. Check each view file that instantiates TimelineEventCard and add the parameter.

**LoomCal/Views/Tasks/TaskRowView.swift** — Add highlight pulse:

1. Use the same HighlightPulseModifier (define it in a shared location, or define a `.highlightPulse(active:)` View extension in one of the files and use it from both). If defining a shared extension, create it in the ActionConfirmationCard file or a new utility file. Simplest approach: define the modifier as a View extension in TimelineEventCard.swift (or a new small file) and import from both places. Actually, Swift files in the same module share namespace — just define the extension once in any file.

Define the View extension once (in TimelineEventCard.swift or a separate HighlightPulse.swift file):
```swift
extension View {
    func highlightPulse(active: Bool) -> some View {
        modifier(HighlightPulseModifier(isHighlighted: active))
    }
}
```

2. Add `isHighlighted: Bool = false` parameter to TaskRowView and apply `.highlightPulse(active: isHighlighted)` at the end of the row body.

3. Update call sites of TaskRowView in TaskListView (or wherever tasks are rendered) to pass `isHighlighted: taskViewModel.highlightedTaskId == task._id`.

**LoomCal/Views/ContentView.swift** — Ensure ChatView has access to the wired ChatViewModel (already does). Double-check that the `.onAppear` block from Plan 02 that sets `chatViewModel.calendarViewModel` and `chatViewModel.taskViewModel` is in place.

Register any new files in pbxproj.
  </action>
  <verify>
Build: `xcodebuild build -project "/Volumes/Blume/Wo Code/Loom Cal/LoomCal.xcodeproj" -scheme LoomCal -destination "platform=iOS Simulator,name=iPhone 16 Pro" -quiet 2>&1 | tail -5`. Verify BUILD SUCCEEDED.
  </verify>
  <done>
ChatView renders ActionConfirmationCard for pending_action messages and ChatBubbleView for regular messages. UndoBanner appears above the input bar during the undo window. TimelineEventCard and TaskRowView have highlight pulse animation that activates when their ID matches the highlighted ID from their respective ViewModels. All call sites updated. Swift builds clean.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify end-to-end Loom action flow</name>
  <files>N/A — human verification checkpoint</files>
  <action>
Human verifies the complete Phase 5 Loom Calendar and Task Actions system:
- Bridge sends calendar/task context to OpenClaw alongside each message
- Loom can propose actions via structured JSON in replies
- Pending action messages render as confirmation cards in the chat stream
- Confirm executes the mutation (event/task CRUD) in real-time
- Cancel dismisses the card without executing
- Undo banner appears for 5 seconds after confirm (for creates and updates)
- Highlight pulse animation on the affected calendar event or task row

Prerequisites:
1. Convex dev server running (`npx convex dev`)
2. Bridge script running on Loom machine with updated loom-bridge.mjs
3. App running on iOS Simulator

Test sequence:

**Test 1 — Create Event:**
1. Open Chat tab
2. Type: "Add a dentist appointment this Thursday at 3pm for 1 hour"
3. Verify: Loom responds with a confirmation card showing event details (title, date, time, duration)
4. Tap "Confirm"
5. Verify: Card updates to "Confirmed" state
6. Verify: Undo banner appears at bottom with countdown
7. Switch to Calendar tab — verify the event appears on Thursday's timeline
8. Verify: The new event has a brief highlight pulse animation

**Test 2 — Create Task:**
1. Open Chat tab
2. Type: "Add a task to buy groceries, high priority, due tomorrow"
3. Verify: Confirmation card with task details
4. Tap "Confirm"
5. Switch to Tasks — verify the task appears with correct priority and due date

**Test 3 — Cancel Action:**
1. Type: "Create an event called Test at noon tomorrow"
2. Verify: Confirmation card appears
3. Tap "Cancel"
4. Verify: Card shows "Cancelled" state
5. Switch to Calendar — verify no "Test" event was created

**Test 4 — Edit Event:**
1. Type: "Move my dentist appointment to 4pm" (referencing the event from Test 1)
2. Verify: Card shows before/after diff (3pm crossed out, 4pm shown)
3. Tap "Confirm"
4. Verify: Calendar shows the updated time

**Test 5 — Delete Event:**
1. Type: "Delete my dentist appointment"
2. Verify: Card shows the event to be deleted
3. Tap "Confirm"
4. Verify: Event disappears from calendar

**Test 6 — Undo:**
1. Type: "Create an event called Undo Test tomorrow at 2pm"
2. Tap "Confirm"
3. Within 5 seconds, tap "Undo" on the banner
4. Verify: Event disappears from calendar, card shows "Undone" state

Expected: All 6 tests pass. If Loom's system prompt needs tuning for reliable ACTION block output, note the issue — that's a prompt refinement, not a code bug.
  </action>
  <verify>User confirms all 6 test scenarios pass or describes issues found.</verify>
  <done>All Loom action flows (create/edit/delete for events and tasks, cancel, undo) verified working end-to-end by human tester.</done>
</task>

</tasks>

<verification>
1. Swift build succeeds on iOS Simulator
2. ActionConfirmationCard.swift renders cards with type-specific icons, details, diff for edits, and Confirm/Cancel buttons
3. UndoBanner.swift shows countdown timer and undo button
4. ChatView conditionally renders ActionConfirmationCard for pending_action messages
5. TimelineEventCard has highlight pulse overlay for highlighted events
6. TaskRowView has highlight pulse overlay for highlighted tasks
7. End-to-end: user asks Loom to create event -> card appears -> confirm -> event on calendar with highlight
</verification>

<success_criteria>
The complete Phase 5 user experience is functional: Loom receives calendar/task context, proposes mutations via confirmation cards in chat, users confirm/cancel/undo, mutations execute in real-time, and affected items pulse with a highlight animation. All 6 test scenarios pass during human verification.
</success_criteria>

<output>
After completion, create `.planning/phases/05-loom-calendar-and-task-actions/05-03-SUMMARY.md`
</output>
