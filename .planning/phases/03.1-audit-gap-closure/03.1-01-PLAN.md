---
phase: 03.1-audit-gap-closure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - LoomCal/Views/Calendar/DayTimelineView.swift
  - LoomCal/Views/Calendar/WeekTimelineView.swift
  - .planning/phases/02-calendar-views/02-VERIFICATION.md
  - .planning/phases/03-task-system/03-02-SUMMARY.md
autonomous: true
requirements:
  - CALV-01
  - CALV-02
  - CALV-03
  - CALV-04
  - CALV-05
  - TASK-06

must_haves:
  truths:
    - "Time-blocked events in DayTimelineView render with orange styling (accent bar + background), not blue"
    - "Time-blocked events in WeekTimelineView render with orange accent bar and orange background tint, not blue"
    - "02-VERIFICATION.md exists at .planning/phases/02-calendar-views/02-VERIFICATION.md confirming all 5 CALV requirements"
    - "03-02-SUMMARY.md frontmatter includes requirements-completed field listing TASK-01 through TASK-04"
  artifacts:
    - path: "LoomCal/Views/Calendar/DayTimelineView.swift"
      provides: "isTimeBlock parameter passed to TimelineEventCard"
      contains: "isTimeBlock: item.event.taskId != nil"
    - path: "LoomCal/Views/Calendar/WeekTimelineView.swift"
      provides: "Inline color switch for time-blocked events"
      contains: "event.taskId != nil"
    - path: ".planning/phases/02-calendar-views/02-VERIFICATION.md"
      provides: "Phase 2 verification with correct filename"
    - path: ".planning/phases/03-task-system/03-02-SUMMARY.md"
      provides: "Requirements traceability for Plan 03-02"
      contains: "requirements-completed"
  key_links:
    - from: "LoomCal/Views/Calendar/DayTimelineView.swift"
      to: "LoomCal/Views/Calendar/TimelineEventCard.swift"
      via: "isTimeBlock parameter in TimelineEventCard initializer"
      pattern: "isTimeBlock: item\\.event\\.taskId != nil"
    - from: "LoomCal/Views/Calendar/WeekTimelineView.swift"
      to: "LoomEvent.taskId"
      via: "inline isTimeBlock detection driving color switch"
      pattern: "event\\.taskId != nil"
---

<objective>
Close all gaps from the v1.0 milestone audit: wire isTimeBlock detection in DayTimelineView and WeekTimelineView so time-blocked events show orange styling, rename Phase 2 verification file to correct filename, and add requirements-completed frontmatter to 03-02-SUMMARY.md.

Purpose: Retroactive audit gap closure — ensures all Phase 2 CALV requirements have verification evidence and all Phase 3 views consistently show time-block visual distinction.

Output: Two Swift view fixes + two documentation fixes. No new files except the renamed verification file.
</objective>

<execution_context>
@/Users/criswo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/criswo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03.1-audit-gap-closure/03.1-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire isTimeBlock in DayTimelineView and WeekTimelineView</name>
  <files>
    LoomCal/Views/Calendar/DayTimelineView.swift
    LoomCal/Views/Calendar/WeekTimelineView.swift
  </files>
  <action>
    Two separate fixes — both views need to detect time-blocked events and render them with orange styling.

    **DayTimelineView.swift** — In the `eventCards(contentWidth:)` method (around line 114-124), find the `TimelineEventCard` initializer call inside the `ForEach(layoutEvents, ...)` loop. Add the `isTimeBlock` parameter between `event:` and `onTap:`:

    ```swift
    TimelineEventCard(
        event: item.event,
        isTimeBlock: item.event.taskId != nil,   // ADD THIS LINE
        onTap: { onEventTap(item.event) },
        ...
    )
    ```

    This is a 1-line addition. TimelineEventCard already accepts `isTimeBlock: Bool = false` and implements full orange/blue styling logic internally. The task icon will appear automatically in day view (acceptable per user discretion — DayTimelineView has room for it unlike narrow week columns).

    **WeekTimelineView.swift** — In the `weekEventView(event:columnWidth:)` method (around lines 234-263), this view does NOT use TimelineEventCard — it has custom inline rendering with hardcoded `Color.blue`. Add three lines at the top of the method body (after the existing `let eventHeight` line):

    ```swift
    let isTimeBlock = event.taskId != nil
    let accentColor: Color = isTimeBlock ? Color.orange.opacity(0.8) : Color.blue
    let bgColor: Color = isTimeBlock ? Color.orange.opacity(0.12) : Color.blue.opacity(0.12)
    ```

    Then replace the two hardcoded color references:
    - `.fill(Color.blue)` on the accent bar RoundedRectangle → `.fill(accentColor)`
    - `.fill(Color.blue.opacity(0.12))` on the background RoundedRectangle → `.fill(bgColor)`

    No task icon in week view — columns are too narrow (as little as 40pt). The orange accent bar alone provides sufficient visual distinction per user discretion.

    Tap behavior is unchanged: tapping a time-blocked event opens EventDetailView (not the linked task) per locked decision.
  </action>
  <verify>
    Build the iOS target: `xcodebuild build -project LoomCal.xcodeproj -scheme LoomCal -destination 'platform=iOS Simulator,name=iPhone 16 Pro' -quiet 2>&1 | tail -5`

    Grep for the isTimeBlock pattern in both files:
    - `grep "isTimeBlock" LoomCal/Views/Calendar/DayTimelineView.swift` should show `isTimeBlock: item.event.taskId != nil`
    - `grep "isTimeBlock\|accentColor\|bgColor" LoomCal/Views/Calendar/WeekTimelineView.swift` should show the three new variables and two substitution sites
  </verify>
  <done>
    DayTimelineView passes `isTimeBlock: item.event.taskId != nil` to TimelineEventCard. WeekTimelineView uses inline `accentColor`/`bgColor` variables driven by `event.taskId != nil` to render time-blocked events with orange styling. iOS build succeeds with no errors.
  </done>
</task>

<task type="auto">
  <name>Task 2: Rename Phase 2 verification file and add 03-02 summary frontmatter</name>
  <files>
    .planning/phases/02-calendar-views/02-VERIFICATION.md
    .planning/phases/03-task-system/03-02-SUMMARY.md
  </files>
  <action>
    Three documentation fixes:

    **1. Rename Phase 2 verification file:**
    ```bash
    git mv ".planning/phases/02-calendar-views/VERIFICATION.md" ".planning/phases/02-calendar-views/02-VERIFICATION.md"
    ```
    No content changes — the file already has complete verification for all 5 CALV requirements (17KB, 5/5 truths verified, 14/14 artifacts verified). Only the filename needs to match the success criterion `02-VERIFICATION.md`.

    After renaming, grep the project for references to the old filename (`VERIFICATION.md` in the `02-calendar-views` directory context) and update any broken references. Check STATE.md and ROADMAP.md specifically.

    **2. Add requirements-completed to 03-02-SUMMARY.md:**
    Add `requirements-completed: [TASK-01, TASK-02, TASK-03, TASK-04]` to the existing YAML frontmatter block in `.planning/phases/03-task-system/03-02-SUMMARY.md`. Insert it as a new line inside the existing `---` block (e.g., after `plan: 02` or before the closing `---`). Do NOT create a second YAML block.

    Rationale for all four TASK IDs: Plan 03-02 built TaskCreationView (TASK-01), TaskDetailView with editing (TASK-02), TaskRowView with completion circle (TASK-03), and TaskDetailView with alert-based delete (TASK-04). It is the primary UI deliverer for all four requirements.

    **3. Confirm CALV-02 checkbox state:**
    Verify that `.planning/REQUIREMENTS.md` line 12 reads `- [x] **CALV-02**: User can view events in a week calendar view`. Current research confirms it is already `[x]`. If already correct, no edit needed — just confirm in the commit message. If somehow not ticked, add the `[x]`.
  </action>
  <verify>
    - `ls .planning/phases/02-calendar-views/02-VERIFICATION.md` confirms the renamed file exists
    - `ls .planning/phases/02-calendar-views/VERIFICATION.md 2>/dev/null` confirms the old file is gone
    - `grep "requirements-completed" .planning/phases/03-task-system/03-02-SUMMARY.md` shows the new frontmatter field
    - `grep "CALV-02" .planning/REQUIREMENTS.md` confirms `[x]` is present
  </verify>
  <done>
    02-VERIFICATION.md exists at correct path with all CALV verification evidence. 03-02-SUMMARY.md has `requirements-completed: [TASK-01, TASK-02, TASK-03, TASK-04]` in frontmatter. CALV-02 checkbox confirmed `[x]` in REQUIREMENTS.md. No broken references to old verification filename.
  </done>
</task>

</tasks>

<verification>
1. iOS build succeeds with no errors (validates Swift code changes)
2. `grep -r "isTimeBlock" LoomCal/Views/Calendar/` shows the pattern in DayTimelineView.swift, WeekTimelineView.swift, and TimelineEventCard.swift (all three views now handle isTimeBlock)
3. `02-VERIFICATION.md` exists at `.planning/phases/02-calendar-views/02-VERIFICATION.md`
4. `03-02-SUMMARY.md` contains `requirements-completed` in its frontmatter
5. All 4 Phase 3.1 success criteria from ROADMAP.md are met:
   - 02-VERIFICATION.md exists and confirms all 5 CALV-* requirements
   - CALV-02 checkbox is [x] in REQUIREMENTS.md
   - WeekTimelineView renders time-blocked events with orange styling
   - 03-02-SUMMARY.md has requirements-completed frontmatter listing TASK-04
</verification>

<success_criteria>
- Time-blocked events (events with non-nil taskId) display orange styling in DayTimelineView, WeekTimelineView, and TodayView (TodayView already works — only Day and Week needed fixing)
- Phase 2 verification file has correct filename matching audit success criterion
- 03-02-SUMMARY.md has requirements traceability via frontmatter
- iOS build compiles without errors
- All v1.0 milestone audit gaps are closed
</success_criteria>

<output>
After completion, create `.planning/phases/03.1-audit-gap-closure/03.1-01-SUMMARY.md`
</output>
