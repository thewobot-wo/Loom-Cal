# Phase 3.1: Audit Gap Closure - Context

**Gathered:** 2026-02-20
**Status:** Ready for planning

<domain>
## Phase Boundary

Close all gaps from v1.0 milestone audit — retroactive Phase 2 verification filename fix, CALV-02 doc fix, WeekView/DayView isTimeBlock integration, and 03-02-SUMMARY.md frontmatter update. No new features.

</domain>

<decisions>
## Implementation Decisions

### isTimeBlock scope
- Fix BOTH WeekTimelineView AND DayTimelineView — not just WeekView
- Both views should pass `isTimeBlock` to `TimelineEventCard` so time-blocked events render with orange styling
- Tap behavior unchanged: tapping a time-blocked event opens EventDetailView (not the linked task)

### isTimeBlock detection pattern
- Claude's discretion on the exact check, but TodayView already uses `event.taskId != nil` as the pattern

### Time-block display
- Claude's discretion on whether to show a task icon alongside the event title (TimelineEventCard already has this capability when `isTimeBlock: true`)

### Summary frontmatter
- Add `requirements-completed` field to 03-02-SUMMARY.md
- Claude determines which requirements plan 02's views contributed to (at minimum TASK-04 per audit)

### Claude's Discretion
- isTimeBlock detection pattern (likely `taskId != nil` matching TodayView)
- Whether time-blocked events show task icon in day/week views
- Which requirements to list in 03-02-SUMMARY.md frontmatter beyond TASK-04
- Verification file naming/content adjustments

</decisions>

<specifics>
## Specific Ideas

No specific requirements — open to standard approaches. The audit success criteria are prescriptive enough to guide implementation.

</specifics>

<deferred>
## Deferred Ideas

- **Dedicated task list/management view** — Currently tasks only appear in TodayView (due today) and as time-blocked calendar events. User wants a standalone task interface for browsing all tasks, filtering by status/priority. This is a new capability that belongs in its own phase.

</deferred>

---

*Phase: 03.1-audit-gap-closure*
*Context gathered: 2026-02-20*
