struct ConvexEnv {
    // Get this URL from .env.local after running `npx convex dev`
    // Run: npx convex dev — follow prompts to link project, then copy CONVEX_URL value here
    // TODO: Use Xcode build configurations (xcconfig) for dev/prod separation in Phase 2+
    static let deploymentUrl = "https://YOUR_DEPLOYMENT.convex.cloud"
}
